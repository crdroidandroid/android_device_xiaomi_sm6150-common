android_app {
    name: "XiaomiDoze",

    srcs: ["src/**/*.java"],

    certificate: "platform",
    platform_apis: true,
    privileged: true,

    static_libs: [
        "org.lineageos.settings.resources",
        "androidx.preference_preference",
    ],
}
